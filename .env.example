# Deployment mode - required by the app (DEBUG or PRODUCTION)
DEPLOYMENT_TYPE=DEBUG

# App secret key (reserved; set only if JWT/session auth is added)
SECRET_KEY=change-me-to-a-random-64-hex-char-string
