services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portfolio
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      PYTHON_GIL: 0
    env_file:
      - .env
